{
  "name": "spendlens-extension",
  "version": "1.0.0",
  "private": true,
  "description": "TokenLens browser extension — see token cost before you send",
  "scripts": {
    "build": "tsup && node scripts/copy-assets.mjs",
    "dev": "tsup --watch"
  },
  "dependencies": {
    "spendlens": "*"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.37",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  }
}
